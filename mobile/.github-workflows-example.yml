# GitHub Actions Workflow for Building AAB
# This is an example workflow - customize as needed

name: Build Android AAB

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'
      - 'mobile/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'frontend/**'
      - 'mobile/**'
  workflow_dispatch:  # Allow manual trigger

jobs:
  build-aab:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          frontend/package-lock.json
          mobile/package-lock.json
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'
    
    - name: Install frontend dependencies
      working-directory: ./frontend
      run: npm ci
    
    - name: Build frontend
      working-directory: ./frontend
      run: npm run build
    
    - name: Install mobile dependencies
      working-directory: ./mobile
      run: npm ci
    
    - name: Sync to Android
      working-directory: ./mobile
      run: npx cap sync android
    
    - name: Make gradlew executable
      working-directory: ./mobile/android
      run: chmod +x ./gradlew
    
    - name: Build unsigned AAB
      working-directory: ./mobile/android
      run: ./gradlew bundleRelease
    
    - name: Upload AAB artifact
      uses: actions/upload-artifact@v3
      with:
        name: app-release-aab
        path: mobile/android/app/build/outputs/bundle/release/app-release.aab
        retention-days: 30
    
    # Optional: Build signed AAB for production
    # Requires secrets: KEYSTORE_FILE, KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD
    # - name: Decode keystore
    #   if: github.ref == 'refs/heads/main'
    #   run: |
    #     echo "${{ secrets.KEYSTORE_FILE }}" | base64 -d > mobile/android/release.jks
    # 
    # - name: Build signed AAB
    #   if: github.ref == 'refs/heads/main'
    #   working-directory: ./mobile/android
    #   env:
    #     RELEASE_STORE_FILE: release.jks
    #     RELEASE_STORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
    #     RELEASE_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
    #     RELEASE_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
    #   run: ./gradlew bundleRelease
    # 
    # - name: Upload signed AAB
    #   if: github.ref == 'refs/heads/main'
    #   uses: actions/upload-artifact@v3
    #   with:
    #     name: app-release-signed-aab
    #     path: mobile/android/app/build/outputs/bundle/release/app-release.aab
    #     retention-days: 90
